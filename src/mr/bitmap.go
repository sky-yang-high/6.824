package mr

// todo: 一个简单的 bitmap 实现
type bitmap struct {
}

//todo: 把 bitmap 中 pos 位置置为 val
// pos 不能超过maxpos
func (b *bitmap) set(pos, val int) {

}

//todo: 判断 bitmap 是否全为 1
func (b *bitmap) isAllSet() bool {

}

// todo: 查找 bitmap 中，在 pos 及 pos 之后，第一个为0的位置
func (b *bitmap) findFirstZeroAfter(pos int) int {

}
